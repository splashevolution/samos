# Build outputs — regenerated by `make`
build/

# Stale ISO copies at repo root (always use build/sam_os.iso)
*.iso

# Serial test log
build/serial.log

# Editor temporaries
*.swp
*.swo
*~
.DS_Store
Thumbs.db
